// Auto-generated by jai-wayland code generator. Do not edit.

//
// ext_background_effect_manager_v1 — background effect factory
//
// This protocol provides a way to improve visuals of translucent surfaces
// by applying effects like blur to the background behind them.
//
// The capabilities are send when the global is bound, and every time they
// change. Note that when the capability goes away, the corresponding effect
// is no longer applied by the compositor, even if it was set before.
//
// Warning! The protocol described in this file is currently in the testing
// phase. Backward compatible changes may be added together with the
// corresponding interface version bump. Backward incompatible changes can
// only be done by creating a new major version of the extension.
//
Ext_Background_Effect_Manager_V1 :: struct {
    id: u32;
    version: u32;
}

// Request opcodes
EXT_BACKGROUND_EFFECT_MANAGER_V1_DESTROY :: 0;
EXT_BACKGROUND_EFFECT_MANAGER_V1_GET_BACKGROUND_EFFECT :: 1;

// Event opcodes
EXT_BACKGROUND_EFFECT_MANAGER_V1_CAPABILITIES :: 0;

Ext_Background_Effect_Manager_V1_Error :: enum u32 {
    // the surface already has a background effect object
    BACKGROUND_EFFECT_EXISTS :: 0;
}

Ext_Background_Effect_Manager_V1_Capability :: enum_flags u32 {
    // the compositor supports applying blur
    BLUR :: 1;
}

// capabilities of the compositor
Ext_Background_Effect_Manager_V1_Capabilities_Args :: struct {
    flags: u32;
}

Ext_Background_Effect_Manager_V1_Event_Kind :: enum u8 {
    CAPABILITIES :: 0;  // capabilities of the compositor
}

Ext_Background_Effect_Manager_V1_Event :: union kind: Ext_Background_Effect_Manager_V1_Event_Kind {
    .CAPABILITIES ,, capabilities: Ext_Background_Effect_Manager_V1_Capabilities_Args;
}

// destroy the background effect manager (destructor)
// Informs the server that the client will no longer be using this
// protocol object. Existing objects created by this object are not
// affected.
ext_background_effect_manager_v1_destroy :: (self: *Ext_Background_Effect_Manager_V1) {
    // TODO(Phase 3): marshal(self, EXT_BACKGROUND_EFFECT_MANAGER_V1_DESTROY, ...); destroy_proxy(self);
}

// get a background effects object
// Instantiate an interface extension for the given wl_surface to add
// effects like blur for the background behind it.
//
// If the given wl_surface already has a ext_background_effect_surface_v1
// object associated, the background_effect_exists protocol error will be
// raised.
ext_background_effect_manager_v1_get_background_effect :: (self: *Ext_Background_Effect_Manager_V1, surface: *Wl_Surface) -> *Ext_Background_Effect_Surface_V1 {
    // TODO(Phase 3): marshal_constructor(self, EXT_BACKGROUND_EFFECT_MANAGER_V1_GET_BACKGROUND_EFFECT, ...);
    return null;
}

EXT_BACKGROUND_EFFECT_MANAGER_V1_INTERFACE :: Interface_Descriptor.{
    name = "ext_background_effect_manager_v1",
    version = 1,
    request_count = 2,
    event_count = 1,
};
