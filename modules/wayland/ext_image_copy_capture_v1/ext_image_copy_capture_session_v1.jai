// Auto-generated by jai-wayland code generator. Do not edit.

//
// ext_image_copy_capture_session_v1 — image copy capture session
//
// This object represents an active image copy capture session.
//
// After a capture session is created, buffer constraint events will be
// emitted from the compositor to tell the client which buffer types and
// formats are supported for reading from the session. The compositor may
// re-send buffer constraint events whenever they change.
//
// To advertise buffer constraints, the compositor must send in no
// particular order: zero or more shm_format and dmabuf_format events, zero
// or one dmabuf_device event, and exactly one buffer_size event. Then the
// compositor must send a done event.
//
// When the client has received all the buffer constraints, it can create a
// buffer accordingly, attach it to the capture session using the
// attach_buffer request, set the buffer damage using the damage_buffer
// request and then send the capture request.
//
Ext_Image_Copy_Capture_Session_V1 :: struct {
    id: u32;
}

// Request opcodes
EXT_IMAGE_COPY_CAPTURE_SESSION_V1_CREATE_FRAME :: 0;
EXT_IMAGE_COPY_CAPTURE_SESSION_V1_DESTROY :: 1;

// Event opcodes
EXT_IMAGE_COPY_CAPTURE_SESSION_V1_BUFFER_SIZE :: 0;
EXT_IMAGE_COPY_CAPTURE_SESSION_V1_SHM_FORMAT :: 1;
EXT_IMAGE_COPY_CAPTURE_SESSION_V1_DMABUF_DEVICE :: 2;
EXT_IMAGE_COPY_CAPTURE_SESSION_V1_DMABUF_FORMAT :: 3;
EXT_IMAGE_COPY_CAPTURE_SESSION_V1_DONE :: 4;
EXT_IMAGE_COPY_CAPTURE_SESSION_V1_STOPPED :: 5;

Ext_Image_Copy_Capture_Session_V1_Error :: enum u32 {
    // create_frame sent before destroying previous frame
    DUPLICATE_FRAME :: 1;
}

// image capture source dimensions
Ext_Image_Copy_Capture_Session_V1_Buffer_Size_Args :: struct {
    width: u32;
    height: u32;
}

// shm buffer format
Ext_Image_Copy_Capture_Session_V1_Shm_Format_Args :: struct {
    format: u32;
}

// dma-buf device
Ext_Image_Copy_Capture_Session_V1_Dmabuf_Device_Args :: struct {
    device: [] u8;
}

// dma-buf format
Ext_Image_Copy_Capture_Session_V1_Dmabuf_Format_Args :: struct {
    format: u32;
    modifiers: [] u8;
}

// all constraints have been sent
Ext_Image_Copy_Capture_Session_V1_Done_Args :: struct {
}

// session is no longer available
Ext_Image_Copy_Capture_Session_V1_Stopped_Args :: struct {
}

Ext_Image_Copy_Capture_Session_V1_Event_Kind :: enum u8 {
    BUFFER_SIZE :: 0;  // image capture source dimensions
    SHM_FORMAT :: 1;  // shm buffer format
    DMABUF_DEVICE :: 2;  // dma-buf device
    DMABUF_FORMAT :: 3;  // dma-buf format
    DONE :: 4;  // all constraints have been sent
    STOPPED :: 5;  // session is no longer available
}

Ext_Image_Copy_Capture_Session_V1_Event :: union kind: Ext_Image_Copy_Capture_Session_V1_Event_Kind {
    .BUFFER_SIZE ,, buffer_size: Ext_Image_Copy_Capture_Session_V1_Buffer_Size_Args;
    .SHM_FORMAT ,, shm_format: Ext_Image_Copy_Capture_Session_V1_Shm_Format_Args;
    .DMABUF_DEVICE ,, dmabuf_device: Ext_Image_Copy_Capture_Session_V1_Dmabuf_Device_Args;
    .DMABUF_FORMAT ,, dmabuf_format: Ext_Image_Copy_Capture_Session_V1_Dmabuf_Format_Args;
    .DONE ,, done: Ext_Image_Copy_Capture_Session_V1_Done_Args;
    .STOPPED ,, stopped: Ext_Image_Copy_Capture_Session_V1_Stopped_Args;
}

Ext_Image_Copy_Capture_Session_V1_Create_Frame_Args :: struct {
}

Ext_Image_Copy_Capture_Session_V1_Destroy_Args :: struct {
}


// create a frame
// Create a capture frame for this session.
//
// At most one frame object can exist for a given session at any time. If
// a client sends a create_frame request before a previous frame object
// has been destroyed, the duplicate_frame protocol error is raised.
ext_image_copy_capture_session_v1_create_frame :: (batch: *MessageBuilder, self: *Ext_Image_Copy_Capture_Session_V1, new_id: u32) {
    args := Ext_Image_Copy_Capture_Session_V1_Create_Frame_Args.{};
    marshal_constructor(batch, self.id, EXT_IMAGE_COPY_CAPTURE_SESSION_V1_CREATE_FRAME, new_id, *args);
}

// delete this object (destructor)
// Destroys the session. This request can be sent at any time by the
// client.
//
// This request doesn't affect ext_image_copy_capture_frame_v1 objects created by
// this object.
ext_image_copy_capture_session_v1_destroy :: (batch: *MessageBuilder, self: *Ext_Image_Copy_Capture_Session_V1) {
    args := Ext_Image_Copy_Capture_Session_V1_Destroy_Args.{};
    marshal(batch, self.id, EXT_IMAGE_COPY_CAPTURE_SESSION_V1_DESTROY, *args);
    // TODO(Phase 4): destroy_proxy(self);
}

EXT_IMAGE_COPY_CAPTURE_SESSION_V1_INTERFACE :: Interface_Descriptor.{
    name = "ext_image_copy_capture_session_v1",
    version = 1,
    request_count = 2,
    event_count = 6,
};
