// Auto-generated by jai-wayland code generator. Do not edit.

//
// wp_color_management_surface_feedback_v1 — color management extension to a surface
//
// A wp_color_management_surface_feedback_v1 allows the client to get the
// preferred image description of a surface.
//
// If the wl_surface associated with this object is destroyed, the
// wp_color_management_surface_feedback_v1 object becomes inert.
//
Wp_Color_Management_Surface_Feedback_V1 :: struct {
    id: u32;
}

// Request opcodes
WP_COLOR_MANAGEMENT_SURFACE_FEEDBACK_V1_DESTROY :: 0;
WP_COLOR_MANAGEMENT_SURFACE_FEEDBACK_V1_GET_PREFERRED :: 1;
WP_COLOR_MANAGEMENT_SURFACE_FEEDBACK_V1_GET_PREFERRED_PARAMETRIC :: 2;

// Event opcodes
WP_COLOR_MANAGEMENT_SURFACE_FEEDBACK_V1_PREFERRED_CHANGED :: 0;
WP_COLOR_MANAGEMENT_SURFACE_FEEDBACK_V1_PREFERRED_CHANGED2 :: 1;

// protocol errors
Wp_Color_Management_Surface_Feedback_V1_Error :: enum u32 {
    // forbidden request on inert object
    INERT :: 0;
    // attempted to use an unsupported feature
    UNSUPPORTED_FEATURE :: 1;
}

// the preferred image description changed (32-bit)
Wp_Color_Management_Surface_Feedback_V1_Preferred_Changed_Args :: struct {
    identity: u32;
}

// the preferred image description changed
Wp_Color_Management_Surface_Feedback_V1_Preferred_Changed2_Args :: struct {
    identity_hi: u32;
    identity_lo: u32;
}

Wp_Color_Management_Surface_Feedback_V1_Event_Kind :: enum u8 {
    PREFERRED_CHANGED :: 0;  // the preferred image description changed (32-bit)
    PREFERRED_CHANGED2 :: 1;  // the preferred image description changed
}

Wp_Color_Management_Surface_Feedback_V1_Event :: union kind: Wp_Color_Management_Surface_Feedback_V1_Event_Kind {
    .PREFERRED_CHANGED ,, preferred_changed: Wp_Color_Management_Surface_Feedback_V1_Preferred_Changed_Args;
    .PREFERRED_CHANGED2 ,, preferred_changed2: Wp_Color_Management_Surface_Feedback_V1_Preferred_Changed2_Args;
}

Wp_Color_Management_Surface_Feedback_V1_Destroy_Args :: struct {
}

Wp_Color_Management_Surface_Feedback_V1_Get_Preferred_Args :: struct {
}

Wp_Color_Management_Surface_Feedback_V1_Get_Preferred_Parametric_Args :: struct {
}


// destroy the color management interface for a surface (destructor)
// Destroy the wp_color_management_surface_feedback_v1 object.
wp_color_management_surface_feedback_v1_destroy :: (batch: *MessageBuilder, self: *Wp_Color_Management_Surface_Feedback_V1) {
    args := Wp_Color_Management_Surface_Feedback_V1_Destroy_Args.{};
    marshal(batch, self.id, WP_COLOR_MANAGEMENT_SURFACE_FEEDBACK_V1_DESTROY, *args);
    // TODO(Phase 4): destroy_proxy(self);
}

// get the preferred image description
// If this protocol object is inert, the protocol error inert is raised.
//
// The preferred image description represents the compositor's preferred
// color encoding for this wl_surface at the current time. There might be
// performance and power advantages, as well as improved color
// reproduction, if the image description of a content update matches the
// preferred image description.
//
// This creates a new wp_image_description_v1 object for the currently
// preferred image description for the wl_surface. The client should
// stop using and destroy the image descriptions created by earlier
// invocations of this request for the associated wl_surface.
// This request is usually sent as a reaction to the preferred_changed
// event or when creating a wp_color_management_surface_feedback_v1 object
// if the client is capable of adapting to image descriptions.
//
// The created wp_image_description_v1 object preserves the preferred image
// description of the wl_surface from the time the object was created.
//
// The resulting image description object allows get_information request.
//
// If the image description is parametric, the client should set it on its
// wl_surface only if the image description is an exact match with the
// client content. Particularly if everything else matches, but the target
// color volume is greater than what the client needs, the client should
// create its own parameric image description with its exact parameters.
//
// If the interface version is inadequate for the preferred image
// description, meaning that the client does not support all the
// events needed to deliver the crucial information, the resulting image
// description object shall immediately deliver the
// wp_image_description_v1.failed event with the low_version cause,
// otherwise the object shall immediately deliver the ready event.
wp_color_management_surface_feedback_v1_get_preferred :: (batch: *MessageBuilder, self: *Wp_Color_Management_Surface_Feedback_V1, new_id: u32) {
    args := Wp_Color_Management_Surface_Feedback_V1_Get_Preferred_Args.{};
    marshal_constructor(batch, self.id, WP_COLOR_MANAGEMENT_SURFACE_FEEDBACK_V1_GET_PREFERRED, new_id, *args);
}

// get the preferred image description
// The same description as for get_preferred applies, except the returned
// image description is guaranteed to be parametric. This is meant for
// clients that can only deal with parametric image descriptions.
//
// If the compositor doesn't support parametric image descriptions, the
// unsupported_feature error is emitted.
wp_color_management_surface_feedback_v1_get_preferred_parametric :: (batch: *MessageBuilder, self: *Wp_Color_Management_Surface_Feedback_V1, new_id: u32) {
    args := Wp_Color_Management_Surface_Feedback_V1_Get_Preferred_Parametric_Args.{};
    marshal_constructor(batch, self.id, WP_COLOR_MANAGEMENT_SURFACE_FEEDBACK_V1_GET_PREFERRED_PARAMETRIC, new_id, *args);
}

WP_COLOR_MANAGEMENT_SURFACE_FEEDBACK_V1_INTERFACE :: Interface_Descriptor.{
    name = "wp_color_management_surface_feedback_v1",
    version = 2,
    request_count = 3,
    event_count = 2,
};
